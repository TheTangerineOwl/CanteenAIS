<UserControl x:Class="CanteenAIS_Views.BaseTemplates.IntFormFieldChecked"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d"
             d:DesignHeight="40" d:DesignWidth="300"
             x:Name="root">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition SharedSizeGroup="Labels" Width="Auto"/>
            <ColumnDefinition SharedSizeGroup="Checks" Width="Auto"/>
            <ColumnDefinition SharedSizeGroup="Values" Width="*" MinWidth="200"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Column="0"
               Style="{StaticResource FormLabelStyle}"
               Content="{Binding LabelText, ElementName=root}"/>
        <CheckBox Grid.Column="1"
                  Style="{StaticResource FormCheckBoxStyle}"
                  IsChecked="{Binding IsChecked, ElementName=root}"/>
        <xctk:IntegerUpDown Grid.Column="2" x:Name="intPicker"
               Style="{StaticResource FormIntPickerStyle}"
               Value="{Binding IntPick, ElementName=root}"
               Minimum="{Binding Minimum, ElementName=root}"
               Maximum="{Binding Maximum, ElementName=root}"
               DefaultValue="{Binding Default, ElementName=root}"
               IsEnabled="{Binding IsChecked, ElementName=root}"/>
    </Grid>
</UserControl>
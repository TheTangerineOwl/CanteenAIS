<Window
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CanteenAIS_Views"
             xmlns:User="clr-namespace:CanteenAIS_ViewModel.ManagementViewModels.User;assembly=CanteenAIS_ViewModel"
             x:Class="CanteenAIS_Views.LoginWindow"
             mc:Ignorable="d"
             ResizeMode="CanMinimize" Background="LightGray" SizeToContent="WidthAndHeight"
             WindowStartupLocation="CenterScreen" Closed="Window_Closed"
             Title="АИС Предприятие общественного питания | Вход" PreviewKeyDown="Window_PreviewKeyDown">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/CanteenAIS_Views;component/Styles/GenericWindowStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Window.DataContext>
        <User:LoginVM/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="100"/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <DockPanel Grid.Row="0" Grid.ColumnSpan="3" LastChildFill="True">
            <Image DockPanel.Dock="Left"
                   Source="{StaticResource LoginLogo}" Stretch="Uniform"
                   MaxHeight="100"/>
            <StackPanel x:Name="textStack" 
                VerticalAlignment="Center"
                DockPanel.Dock="Right">
                <TextBlock Text="АИС Предприятие общественного питания"
                   FontSize="16" Margin="5" Padding="2"
                   TextAlignment="Right"
                   TextWrapping="Wrap"
                   Background="PaleGreen"/>
                <TextBlock Text="Версия 1.0.0.1"
                   FontSize="16" Margin="5" Padding="2"
                   TextAlignment="Right"
                   Background="PaleGoldenrod"/>
                <TextBlock Text="Вход"
                   FontSize="16" Margin="5" Padding="2"
                   TextAlignment="Right"
                   Background="LightYellow"/>
            </StackPanel>
        </DockPanel>
        <Label Grid.Row="1" Grid.Column="0" Content="Логин" HorizontalAlignment="Right" Style="{StaticResource LabelStyle}"/>
        <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Style="{StaticResource TextBoxStyle}"
                 Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 />
        <Label Grid.Row="2" Grid.Column="0" Content="Пароль" HorizontalAlignment="Right" Style="{StaticResource LabelStyle}"/>
        <PasswordBox x:Name="passBox" Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2"
                     Style="{StaticResource PasswordBoxStyle}" PasswordChanged="PasswordBox_PasswordChanged"
                     />
        <Button Content="Логин" Style="{StaticResource DialogButtonStyle}" Grid.Row="3" Grid.Column="1"
                Command="{Binding ClickLogin}" IsDefault="True"/>
        <Button Content="Отмена" Style="{StaticResource DialogButtonStyle}" Grid.Row="3" Grid.Column="2"
                IsCancel="True"
                Command="{Binding ClickCancellation}"/>
        <StatusBar Grid.Row="4" Grid.ColumnSpan="3">
            <Label Content="{Binding InputInfo}"/>
        </StatusBar>
    </Grid>
</Window>